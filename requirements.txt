# Core RAG
chromadb>=0.5.0
sentence-transformers>=3.0.0
pymupdf>=1.24.0

# LLM API (replaces Ollama)
groq>=0.9.0

# Database (replaces SQLite)
supabase>=2.0.0
psycopg2-binary>=2.9.0

# Telegram bot
python-telegram-bot[job-queue]>=21.0

# Utilities
python-dotenv>=1.0.0
requests>=2.31.0

# Web server (for health checks on Render)
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
